spring:
  application:
    name: api-gateway
  # cloud:
  #   gateway:
  #     routes:
  #       - id: user-service
  #         uri: http://localhost:8081
  #         predicates:
  #           - Path=/api/v1/user/**
  #         filters:
  #           - name: JwtAuthenticationFilter
