spring:
  devtools:
    restart:
      enabled: true
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    # properties:
    #   hibernate:
    #     format_sql: true

# springdoc:
#   swagger-ui:
#     path: /swagger-ui.html

logging:
  level:
    root: INFO
    site:
      dlink:
        auth: DEBUG